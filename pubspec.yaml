name: setup
description: A new Flutter project.preferred for private packages.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Utils
  flutter_localizations:
    sdk: flutter
  google_fonts: ^4.0.3
  dio: ^4.0.6
  pretty_dio_logger: ^1.1.1
  intl: ^0.17.0
  carousel_slider: ^4.2.1
  sliding_up_panel: ^2.0.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  generate: true
  uses-material-design: true